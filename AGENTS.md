# めいな開発ルール

このリポジトリでは、Codex / GPT系コーディングエージェントがめいなの開発を主担当として進める。

## 基本方針

- ユーザーは「何を作りたいか」を伝え、実装・調査・テスト・修正は可能な限りエージェントが担当する。
- 既存機能を壊さないことを最優先する。
- 大規模な変更は、目的ごとに小さな変更へ分割する。
- 推測だけで既存コードを書き換えず、まず関連ファイルを読んで現在の実装を確認する。
- 変更前に重要ファイルのバックアップまたはGitの履歴で復旧可能な状態を確保する。
- 変更後は、可能な範囲で構文チェック・セルフテスト・関連テストを実行する。
- テストが実行できない場合は、実行できなかった理由を最終報告に明記する。
- 変更内容とテスト結果をコミットメッセージや最終報告で明確にする。

## めいなの主要構成

- `meina_agent.py`: 音声入力、Whisper、TTS、会話、AI判断、PC操作を統合する本体
- `command_router.py`: PC/Web操作を安全に振り分けるルーター
- `meina_brain/brain_core.py`: 自作AIブレイン
- `meina2/tools.py`: PC/Web操作の実装
- `self_test.py`: 実行時依存を含む軽量セルフテスト
- `run_meina.py`: 実行環境とCUDA DLLを整えて本体を起動
- `twitch_clip_pipeline.py`: Twitch VOD取得、音声抽出、Whisper文字起こし
- `twitch_ai_clipper.py`: AIによる切り抜き候補選定と切り抜き作成
- `twitch_video_editor.py`: 字幕、映像効果、編集、任意BGMを含む動画編集
- `meina_twitch.py` / `meina_twitch_voice.py`: Twitch機能のめいな連携
- `start_meina.bat`: 起動時の自動アップグレードと本体起動

## 変更ルール

1. 変更対象と依存関係を確認する。
2. 既存のバックアップ・アップグレード処理を確認する。
3. 必要最小限のファイルを変更する。
4. 危険な任意シェル実行、任意ファイル削除、無制限のマウス・キーボード操作などを新しいルートとして追加しない。
5. 秘密情報をコードへハードコードしない。
6. Twitch Client Secret、ローカル状態、VOD、生成動画、仮想環境、キャッシュをGitへ追加しない。
7. 音声・Twitch・動画編集など時間のかかる処理は、既存の同期動作を不用意に壊さない。非同期化する場合は既存動作を維持しながら段階的に行う。
8. 既存APIや関数名を変更する場合は、呼び出し元をすべて確認してから変更する。
9. Windowsのパスや日本語ファイル名を前提にテストする。
10. GitHub上で変更する場合も、テスト可能な最小単位でコミットする。

## テスト方針

最低限、可能な場合は以下を実行する。

```powershell
python -m py_compile meina_agent.py command_router.py meina2\tools.py meina_brain\brain_core.py
python self_test.py
```

さらに変更した機能に対応する専用テストを追加する。

GPU、Whisper、Ollama、TTS、OBS、Twitch VOD、FFmpegなど実PC依存の機能は、CIだけで完全には検証できない。その場合は静的テストと実PCテストを分ける。

## Git / GitHub方針

- `main` を安定版として扱う。
- 変更理由が分かるコミットメッセージを使う。
- 大きな変更では、可能なら機能単位でコミットする。
- 既存の動作を確認せずに履歴を書き換えたり、force pushしたりしない。
- 秘密情報を含むファイルをコミットしない。
- PRを使う場合は、変更内容・テスト結果・既知の制約を本文に残す。

## エージェントの作業スタイル

ユーザーが明確に「作って」「直して」「進めて」と依頼した場合、不要な確認を何度も挟まず、調査→実装→テスト→修正まで可能な範囲で連続して進める。

ただし、外部サービスへの不可逆な操作、秘密情報の公開、データ削除、権限変更などの重大操作は勝手に実行しない。

## 完成条件

機能追加は「コードを書いた」だけで完了とせず、可能な範囲で以下を満たす。

- 既存コードとの整合性がある
- 構文エラーがない
- 関連する自動テストが通る
- 失敗時のフォールバックがある場合は維持されている
- README等の利用手順が必要なら更新されている
- GitHub上で変更履歴が追跡できる
- ユーザーに変更点とテスト結果を短く報告できる
